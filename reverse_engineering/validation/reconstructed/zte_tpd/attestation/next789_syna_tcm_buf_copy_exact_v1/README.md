# syna_tcm_buf_copy — offline exact promotion

Stock entry `0011ce6c`, body `640` bytes. The candidate reproduces the AArch64 instruction and relocation surface and has no valid KCFI preamble on either stock or candidate. Canonical Docker build, strict Joern and two ASan/UBSan host cycles passed. Hardware validation remains deferred.
