# syna_dev_resume — offline exact attestation

This attestation covers microtask 163 at stock entry `00112d6c`. The canonical module was built in two independent Docker cycles. The exact AArch64 object matches the preserved stock function at 1548 bytes and 387 instructions, including relocations, printk formats and KCFI type ID `0x2a703c0b`.

The host contract harness covers six resume, reset, event, firmware and configuration cases under ASan/UBSan in two reproducible cycles. Hardware and Android power-management integration remain deferred.
