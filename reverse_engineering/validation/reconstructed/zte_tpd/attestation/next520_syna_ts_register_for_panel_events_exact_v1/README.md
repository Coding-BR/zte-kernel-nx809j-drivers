# `syna_ts_register_for_panel_events` exact offline attestation

Microtask 165, stock entry `00113504`, is promoted only after the source-sync, two-cycle canonical Docker build, strict Joern, two-cycle ASan/UBSan harness, KCFI, and AArch64 gates passed.

The stock function has no valid KCFI entry preamble. The candidate uses the object-level `-fno-sanitize=kcfi` flag recorded in the reconstructed `Makefile`, and both KCFI reports therefore agree on the absent preamble and 136-byte symbol size.

Physical hardware validation remains deferred.
