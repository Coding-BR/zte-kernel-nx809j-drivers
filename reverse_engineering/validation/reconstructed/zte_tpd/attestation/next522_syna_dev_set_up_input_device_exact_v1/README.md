# `syna_dev_set_up_input_device` exact offline attestation

Microtask 167, stock entry `001136c4`, is promoted after source-sync, two-cycle canonical Docker build, strict Joern, two-cycle ASan/UBSan harness, KCFI, and AArch64 gates passed.

The candidate matches the stock function's 245 AArch64 instructions, relocations, 980-byte symbol size, printk formats, and absent KCFI preamble. Physical hardware validation remains deferred.
