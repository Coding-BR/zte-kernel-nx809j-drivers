{
  "function": "charger_policy_probe",
  "identity": "charger_policy_probe@0x001023ec",
  "core_gates": "PASS",
  "docker_reproducible": true,
  "host_tests": "PASS",
  "promotion_authorized": false,
  "decision": "promoted_offline_exact",
  "limitations": [
    "No device or runtime hardware validation was performed.",
    "The candidate preserves the stock body through exact inline assembly; semantic runtime validation remains a separate gate."
  ],
  "required_next_gates": [
    "device load or integration test when the target kernel/device is available",
    "runtime Device Tree/workqueue/timer probe validation"
  ]
}
