# Exact attestation: `_inline_copy_to_user`

Target: `_inline_copy_to_user@0x0010133c`, a compiler-generated helper emitted by `copy_to_user` inside `gf_ioctl`. The stock body is 172 bytes and 43 AArch64 instructions.

The candidate matches the stock helper at the instruction, relocation, ELF section and symbol-size levels. The observed relocation is the call to `__arch_copy_to_user`, and the implementation retains the stock user-pointer masking and TTBR/DAIF transition sequence generated by the kernel headers.

Validation result: `CORE_GATES_PASS`. Assembly, input identity, map identity, two clean Docker builds, Joern scoped analysis, Joern usage slices and KCFI comparison passed. The stock and candidate KCFI extractor both record `NO_VALID_KCFI_PREAMBLE`; the apparent preamble overlaps `nav_event_input+0x1cc`, so KCFI is explicitly N/A for this direct compiler-generated helper. The 30-function host harness passed reproducibly.

The independent Docker evidence adapter still reports its separate acquisition-chain limitation because the stock module/Ghidra export and transition document were not copied into that run root. This is offline/static evidence only and makes no hardware-execution or 100% equivalence claim.
